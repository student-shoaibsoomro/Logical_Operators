//  Logical Operators:
//  (And) It returns true if the both sides are true
//  otherwise answer it returns False.

let age = 20;
let income = 40000;
console.log((age >= 18) && (income >= 30000));

// (Or) It returns True if atleast one of the sides
//  are true otherwise it returs False.

let age1 = 25;
let income1 = 20000;
console.log((age >= 18) || (income >= 35000));

// (Not) It returns the opposite Boolean value of the operand,
// if the operand is true it returs false and if the operand is
// false, It returns True.
let a = 20;
let b = 30;
console.log(a !< b);